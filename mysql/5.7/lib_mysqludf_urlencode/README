README $Revision: 1.3 $
=======================

HUMANAGE(int datetime1[, int datetime2=NOW()[, precision=4])

	HUMANAGE() calculates the duration of two date(time)s where precision may
	be any value of 4 (years), 6 (months), 8 (days), 10 (hours), 12 (minutes)
	and 14 (seconds).
	
	Examples:
		HUMANAGE(2005, 2007)							= 2
		HUMANAGE(2005, 2007, 8)							= 670
		HUMANAGE(20050101000001, 20041231235959, 14)	= 2
	
	Note:
		This function should probably be named DURATION().


URLDECODE(string encoded)

	URLDECODE() decodes an urlencoded string.


URLENCODE(string encoded)

	URLENCODE() encodes a string suitable for URL usage (query string).
